## KV Cache

KV cache 是什么，为什么要有 KV cache, KV cache 为什么会造成显存过大的问题，如何解决？

### Why KV cache

### How KV cache works

kv cache 拼接

```python
def custom_attention(query, key, value, past_key_value=None):
    # key: [b, s, d]
    # value: [b, s, d]
    # past_key_value: [b, s', d]
    if past_key_value is not None:
        past_key, past_value = past_key_value
        key = torch.cat([past_key, key], dim=-2)
        value = torch.cat([past_value, value], dim=-2)

    attention_weights = query @ key.transpose(-2, -1)

    ...

    return output, (key, value)
```

dynamic cache

```python
class DynamicCache(Cache):
    def __init__(self) -> None:
        self.key_cache = List[torch.Tensor] = []
        self.value_cache = List[torch.Tensor] = []

    def update(self, key_states, value_states, layer_idx, cache_kwargs):
        if layer_idex >= len(self.key_cache):
            self.key_cache.append(key_states)
            self.value_cache.append(value_states)
        else:
            self.key_cache[layer_idx] = torch.cat([self.key_cache[layer_idx], key_states], dim=-2)
            self.value_cache[layer_idx] = torch.cat([self.value_cache[layer_idx], value_states], dim=-2)


        return self.key_cache[layer_idx], self.value_cache[layer_idx]
```

attention 调用

```python
class Attention(nn.Module):
    def forward(self, hidden_states, past_key_value=None, use_cache=False, layer_idx=None):
        query = self.q_proj(hidden_states)
        key = self.k_proj(hidden_states)
        value = self.v_proj(hidden_states)

        # ...
        if past_key_value is not None:
            key, value = past_key_value.update(key, value, layer_idx, cache_kwargs)

        attention_output = F.scaled_dot_product_attention(query, key, value)

        if use_cache:
            return attention_output, past_key_value

        return attention_output
```

其他 cache:

1. `DynamicCache`: 默认实现，动态增长，灵活性高
2. `StaticCache`: 预分配固定大小的张量，与 `torch.compile` 结合更好，可以减少动态形状带来的开销
3. `QuantizedCache`: 量化缓存，对 K/V 进行量化来节省显存
4. `PagedAttentionCache`: 借鉴 vLLM 的分页注意力思想，管理非连续显存块

```python
class PagedAttentionCache:
    def __init__(self, block_size=16):
        self.physical_blocks = []
        self.block_tables = {}

    def allocate(self, sequence_id, num_tokens):
        logical_blocks = []
        for _ in range((num_tokens + block_size - 1) // block_size):
            block = self.physical_blocks.pop()
            logical_blocks.append(block)

        self.block_tables[sequence_id] = logical_blocks

    def get_kv(self, sequence_id, token_idx):
        block_idx = token_idx // block_size
        offset = token_idx % block_size
        physical_block = self.block_tables[sequence_id][block_idx]
        return self._read_block(physical_block, offset)
```

### Analysis

KV cache 会把 decoding 过程从 GEMM 变成 GEMV (General Matrix-Vector Multiply)

### How to optimize KV cache

- Shared KV: MQA [@MQA], GQA [@GQA]
- Windowed KV: Longformer
- Quantized Compression: KVQuant
- infra上：paged attention，kv cache offloading
