import Note from '../../../components/Note.astro'
import Accordion from '../../../components/Accordion.astro'
import HtmlEmbed from '../../../components/HtmlEmbed.astro';

## Data Parallelism

data parallesim (DP) 的核心思想是

<Note title="Core Idea" emoji="💡" variant="info">
  将模型复制到多个 GPU 上，每个 GPU 同时运行不同的 micro batches.
</Note>

### Naive DP

假如我们现在要训练一个模型 $f(x,y;\theta)$, 其中 $x,y,\theta$ 分别是输入，输出和训练参数。
给定训练集 $\mathcal{D}=\{(x_i,y_i)\}_{i=1}^M$, 我们的训练目标如下

$$
\mathcal{L}(x, y;\theta, \mathcal{D}) = \frac{1}{M}\sum_{i=1}^M f(x_i, y_i;\theta)
$$

使用 DP 之后，假如我们现在有 $N$ 个 GPU, 我们将模型 $f(x,y;\theta)$ 复制到这 $N$ 个 GPU 上，得到 $f_1=\dots=f_N=f$, 然后每个 GPU 就可以处理大小为 $M/N$ 的 micro batch:

$$
\mathcal{L}(x, y;\theta, \mathcal{D}) = \frac{1}{N}\sum_{i=1}^N\mathcal{L}(x, y;\theta, \mathcal{D}_i)=\frac{1}{N}\sum_{i=1}^N\frac{N}{M}\sum_{j=1}^{M/N} f_i(x_j,y_j;\theta)
$$

但是这也意味着不同的 GPU 实例上有不同的 loss 和梯度，第 $i$ 个 GPU 上的梯度为

$$
\nabla_\theta\mathcal{L}(x, y;\theta, \mathcal{D}_i) = \sum_{i=1}^G\frac{1}{N_G}\sum_{j=1}^{N_G} \nabla_\theta f_i(x_j,y_j;\theta),i=1,\dots,N
$$

为了解决模型同步的问题，我们使用 [AllReduce](#all-reduce) 来统一反向传播的梯度

$$
\mathrm{AllReduce}([\nabla_\theta\mathcal{L}(x, y;\theta, \mathcal{D}_1),\dots,\nabla_\theta\mathcal{L}(x, y;\theta, \mathcal{D}_G)])
$$

这样每个 DP rank 上的模型更新就是

$$
f_i(x,y;\theta_{k+1}) = f_i(x,y;\theta_{k}) - \alpha\nabla_\theta\mathcal{L}(x, y;\theta, \mathcal{D}),i=1,\dots,G
$$

从而我们保证了模型每一次更新之后，在不同的 DP rank 上都是一致的。

将上面的流程进行汇总，我们就得到 DP 的计算过程：

1. 每个 GPU 单独进行 forward pass 得到 loss
2. 每个 GPU 单独进行 backward pass 得到对应的梯度
3. 通过 AllReduce 来同步不同 GPU 上的梯度
4. 通过 optimizer states 来获取参数更新
5. 使用参数更新各个 GPU 上的梯度

对应的 pipeline 如下所示：

<HtmlEmbed
    src="naive_dp_pipeline.html"
    title="Naive DP pipeline"
/>

通信开销只有一部分：在 backward pass 时，对梯度进行一次 ReduceScatter 同步不同 GPU 上的梯度

#### Optimized naive DP

Naive DP 的逻辑非常简单，不同的 DP rank 完成 forward pass, backward pass, AllReduce, 权重更新。
但是在 AllReduce 的过程中，我们发现 GPU 出现了 bubble, 也就是 idle time, 此时其他 GPU 都在等待 gradients 传输完成才会进行 optimizer step 的相关计算。

注意到，我们在进行反向传播的时候， 其实并不需要等待反向传播完成再进行 AllReduce, 而是可以在某一个参数完成反播之后立马就进行 AllReduce 来同步这个参数的梯度给其他 GPU.
也就是说，我们将粗粒度的 model level gradient AllReduce 细化到了 parameter level gradient AllReduce.

下面是对应的 pipeline:

<HtmlEmbed
    src="naive_dp_fine_grained.html"
    title="Naive DP pipeline with fine-grained AllReduce"
/>

可以看到，相比于 naive DP, 我们现在通过计算通信重叠降低了整体的训练时间。

下面是对应的实现

<Accordion title="Fine-gradined AllReduce Optimization on Naive DP">

```python
class DataParallelNaive(nn.Module):
    def __init__(self, module):
        super().__init__()
        self.module = module
        self.require_backward_grad_sync = True
        self.register_backward_hook(self._all_reduce_grads)
    
    def forward(self, *inputs, **kwargs):
        return self.module(*inputs, **kwargs)

    def register_backward_hook(self, hooks):
        for p in self.module.parameters():
            if p.requires_grad is True:
                p.register_hook(hook)

    def _all_reduce_grads(self, grad):
        if self.require_backward_grad_sync:
            dist.all_reduce(grad, op=dist.ReduceOp.SUM, group=pgm.process_group_manager.cp_dp_group)
            grad /= pgm.process_group_manager.cp_dp_word_size
        
        return grad
    
    @contextlib.contextmanager
    def no_sync(self):
        self.require_backward_grad_sync = False
        yield
        self.require_backward_grad_sync = True
```

</Accordion>

Parameter level AllReduce 确实优化了 GPU idle time, 但是 GPU 的计算与通信更偏好 large tensor.
因此我们做的第二个优化就是 bucketing, 核心思想就是把一些小的 graeitn tensor 进行打包，然后在进行 AllReduce, 即 bucket level AllReduce.

对应的 pipeline 如下所示：

<HtmlEmbed
    src="naive_dp_bucket.html"
    title="Naive DP pipeline with Bucket AllReduce"
/>

下面是对应的实现

<Accordion title="Bucket AllReduce Optimization on Naive DP">

```python
class DataParallelBucket(nn.Module):
    def __init__(self, module, bucket_cap_mb=25, grad_type=torch.float32):
        super().__init__()
        self.module = module
        self.require_backward_grad_sync = True
        grad_size = 2 if grad_type == torch.bfloat16 else 4
        bucket_size = bucket_cap_mb * 1024 * 1024 // grad_size
        self.bucket_manager = BuckerManager(module.parameters(), pgm.process_group_manager.cp_dp_group, bucker_size, grad_type)
        self.register_backward_hook()
        self._post_backward_callback_set = False
    
    def forward(self, *inputs, **kwargs):
        return self.module(*inputs, **kwargs)

    def backward(self, input_tensor, output_tensor, output_tensor_grad):
        return self.module.backward(input_tensor, output_tensor, output_tensor_grad)

    def register_backward_hook(self, hooks):
        self.grad_accs = []
        for p in self.module.parameters():
            if p.requires_grad is True:
                param_tmp = param.expand_as(param)
                grad_acc_fn = param_tmp.grad_fn.next_functions[0][0]
                grad_acc_fn.register_hook(self._make_param_hook(param, self.bucket_manager))
                self.grad_accs.append(grad_acc_fn)

    def __make_param_hook(self, params, bucket_manager):
        def param_hook(*unused):
            if param.requires_grad:
                assert param.grad is not None
                param.main_grad.add_(param.grad.data)
                param.grad = None

                if self.require_backward_grad_sync:
                    if not self._post_backward_callback_set:
                        Variable._execution_engine.queue_callback(self._post_backward)
                        self._post_backward_callback_set = True

                    
                    bucket_manager.mark_param_as_ready(param)

        return param_hook

    @contextlib.contextmanager
    def no_sync(self):
        self.require_backward_grad_sync = False
        yield
        self.require_backward_grad_sync = True

    def _post_backward(self):
        self.bucket_manager.wait()
        self._post_backward_callback_set = False
        for p in self.module.parameters():
            if p.requires_grad:
                p.grad = p.main_grad.to(p.dtype)

    def reset(self)
    self.bucket_manager.reset()
```

</Accordion>

第三个优化是针对 gradient accumulation 的。
对于 [gradient accumulation](#gradient_accumulation), 我们仅在 $K$ 步 ($K=$ gradient accumulation size) 之后才会进行一次 optimizer states.

如果我们每次反向传播都通过一次 AllReduce 来同步梯度的话，显然降低了整体的计算效率，因为前面 $K-1$ 步实际上不需要梯度同步。

为了解决这个问题，Pytorch 使用 `no_sync` 来让用户控制是否同步梯度，通过 `no_sync` 我们可以避免前 $K-1$ 步的 `backward()` 进行 AllReduce.

DeepSpeed 则是通过一个状态机来进行 AllReduce 的控制，具体做法就是维护一个计数变量 `micro_step_id`，代表当前的 micro-batch size, 当 `micro_step_id` $<K$ 时，我们不进行 AllReduce.
伪代码如下所示

<Accordion title="Gradient Accumulation in DeepSpeed (pesudo code)">

```python
class DeepSpeedEngine:
    def __init__(self, config):
        self.grad_acc_steps = config.gradient_accumulation_steps
        self.micro_step_id = 0

    def is_boundary(self):
        return (self.micro_step_id + 1) % self.grad_acc_steps == 0

    def backward(self, loss):
        loss.backward()

        self.micro_step_id += 1

    def register_zero2_hooks(self, model):
        for layer in model.layers:
            def layer_backward_hook(grad_output):
                if not self.is_boundary():
                    return None # disable sync
                
                trigger_asynchronous_reduce_scatter(layer.grad)
                release_unowned_gradient_memory(layer)
            
            layer.register_backward_hook(layer_backward_hook)
```

</Accordion>

#### Practice

最后，我们回顾一下 batch size 定义

$$
bs=gbs = mbs\times grad_{acc}\times DP
$$

优化时，有以下几点：

1. 优先考虑 DP 而不是 gradient accumulation
2. GPU 非常多时，使用较小的 $mbs$, 因为这样可以加速训练

### ZeRO

在上一节我们介绍了如何使用多个 GPU 来实现 DP.
我们这样做有一个内在假设：模型权重可以被放到单个 GPU 里。
但是，现在大部分模型都不能放到一个 GPU 里，一个 80GB HBM 内存的 GPU 最多只能放下一个 40B (BF16精度) 的模型，而现在大部分模型都到了 $\sim100$ B的级别。
因此，我们需要对现有的 DP 算法进行优化。

第一个算法就是微软提出的 ZeRO [@ZeRO]. ZeRO 的核心思想为

<Note title="Core Idea" emoji="💡" variant="info">
  将训练时的模型参数，梯度，优化器状态复制到多个 GPU 上，来减少 DP 的内存占用。
</Note>

对于 Naive DP, 我们的每一个 DP rank 里都保留一份完整的参数，梯度，优化器状态用于前向，反向传播。
可以看到，不同的 DP rank 实际上存储的东西是彼此的副本，这显然带来了很大的冗余。
因此， ZeRO 就想办法来避免这些冗余。

假设模型参数量为 $\Psi$, 在训练过程中，我们需要保存以下信息：

1. 模型参数： BF16 精度, $2\Psi$
2. 模型梯度： BF16, $2\Psi$
3. master weights: FP32, $4\Psi$
4. optimizer states: Adam, FP32, $4\Psi+4\Psi$
5. (optional) accmulcated gradients, FP32, $4\Psi$

本节中，我们暂时不考虑 accumulated gradients, 因此整体的内存占用就是

$$
2\Psi+2\Psi+12\Psi.
$$

ZeRO 的思想就是将这些不同的信息切分到不同的 DP rank 上进行分块保存，进而降低内存消耗。

TODO: 框架图

假设我们现在有 $N$ 个 GPU, 我们分别来介绍不同的 ZeRO 算法

#### ZeRO-1

对于 ZeRO-1 来说，我们会把 optimizer states 均匀切分到 $N$ 个 GPU上，这样每个 GPU 保存的就是 $k\Psi/N$ (这里我们用 $k$ 来表示不同 optimizer 对应的系数，对 Adam 来说是
$12$).

训练时，我们通过 AllGather 来收集不同 GPU 上的 optimizer states 再进行计算：

1. 前向传播计算 loss
2. 反向传播计算梯度
3. 进行一次 ReduceScatter 在不同 GPU 之间同步梯度
4. 每个 GPU 进行一次 optimizer step, 得到局部更新的 optimizer states 以及权重更新量
5. 对权重更新量进行一次 AllGather 来更新模型参数

训练流程如下所示：

<HtmlEmbed
    src="ZeRO1.html"
    title="ZeRO-1 training"
/>

pipeline 如下所示：

<HtmlEmbed
    src="ZeRO1-pipeline.html"
    title="ZeRO-1 pipeline"
/>

通信开销包括两部分：

1. backward pass 时，对梯度进行一次 ReduceScatter 同步不同 GPU 上的梯度
2. optimizer step 之后，通过 AllGather 来同步不同 GPU 上的参数

因此 ZeRO-1 的通信量为 $\Psi+\Psi=2\Psi$.

现在，每个 GPU 上有完整的参数，完整的梯度和切分后的 optimizer states, 因此每个 GPU 上的内存占用为

$$
2\Psi + 2\Psi + \frac{k\Psi}{N}
$$

与 naive DP 一样，我们有两种优化方式：

1. during the optimizer step: TODO
2. during the forward pass: TODO

#### ZeRO-2

ZeRO-2 在 ZeRo-1 的基础上，进一步把 gradient 也进行了 sharding.

训练时，我们通过 AllGather 来收集不同 GPU 上的 optimizer states 和 gradients 再进行计算：

1. 前向传播计算 loss
2. 反向传播计算梯度
3. 进行一次 ReduceScatter 同步不同 GPU 上的梯度
4. 每个 GPU 进行一次 optimizer step, 得到局部更新的 optimizer states 以及权重更新量
5. 对权重更新量进行一次 AllGather 来更新不同 GPU 上的模型参数

训练流程如下所示：

<HtmlEmbed
    src="ZeRO2.html"
    title="ZeRO-2 training"
/>

训练 pipeline 对应为

<HtmlEmbed
    src="ZeRO2-pipeline.html"
    title="ZeRO-2 pipeline"
/>

通信开销包括两部分：

1. backward pass 时，对梯度进行一次 ReduceScatter 同步不同 GPU 上的梯度
2. optimizer step 之后，通过 AllGather 来同步不同 GPU 上的参数

因此 ZeRO-2 的通信量为 $\Psi+\Psi=2\Psi$.

现在，每个 GPU 上有完整的参数，切分后的梯度和 optimizer states, 因此每个 GPU 上的内存占用为

$$
2\Psi + \frac{2\Psi + k\Psi}{N}
$$

ZeRO-2 和 ZeRO-1的区别在于，我们会实时进行通信以及释放内存：

#### ZeRO-3

ZeRO-3 则进一步将模型参数也进行了 sharding, Pytorch 将 ZeRO-3 的原生实现称之为 Fully Sharded Data Parallelism (FSDP) [@FSDP; @SimpleFSDP].

ZeRO-3 在 forward pass 中，先使用 AllGather 收集所需参数再进行计算，计算完之后，收集的参数会被立即释放。
Backward pass 时，也是一样的道理

ZeRO-3 的计算流程为：

1. 前向传播时，对每一层，先通过 AllGather 收集当前层的参数进行前向传播，计算完毕之后然后立刻通过 flush 释放掉当前层的参数
2. 反向传播时，对每一层，先通过 AllGather 收集当前层的参数进行反向传播，然后
   1. 计算完毕之后立刻通过 flush 释放掉当前层的参数
   2. 通过 ReduceScatter 将梯度同步到不同的 GPU 上
3. Optimizer step, 每个 GPU 上单独更新本地的参数

如下图所示：

<HtmlEmbed
    src="ZeRO3.html"
    title="ZeRO-3 training"
/>

计算的 pipeline 为

<HtmlEmbed
    src="ZeRO3-pipeline.html"
    title="ZeRO-3 pipeline"
/>

通信开销包括三部分:

1. forward pass 时，对参数进行一次 AllGather
2. backward pass 时，对参数进行一次 AllGather
3. backward pass 时，对梯度进行一次 ReduceScatter 同步不同 GPU 上的梯度

因此 ZeRO-3 的通信量为 $\Psi+\Psi+\Psi=3\Psi$,

内存开销：

$$
\frac{2\Psi+2\Psi+k\Psi}{N}
$$

优化： 在 DP 不太大的时候可以使用 prefetching, forward pass 时提前收集下一层的权重进行计算

#### Analysis on ZeRO

按照通信来对比，我们可以得出如下结果

|         Strategy         |       naive DP      |             ZeRO-1            |             ZeRO-2            |             ZeRO-3            |
|:------------------------:|:-------------------:|:-----------------------------:|:-----------------------------:|:-----------------------------:|
| Forward Pass             | None                  | None              |  None             |  AllGather Parameters $\Psi$         |
| Backward Pass            | ReduceScatter gradients   $\Psi$     | ReduceScatter gradients   $\Psi$             |  ReduceScatter gradients   $\Psi$             |  AllGather Parameters $\Psi$ +ReduceScatter gradients   $\Psi$         |
| Optimizer Step           | None                  | None              |  None            |  None         |
| Parameter Update         | None                  | AllGather Parameters $\Psi$              |  AllGather Parameters $\Psi$              |  None         |
| Total                    | $\Psi$                 | $2\Psi$              |  $2\Psi$              |  $3\Psi$         |

实际上，如果我们更改 ZeRO-1 和 ZeRO-2 的参数更新时间，将其放在下一次迭代的 forward pass 开始时，我们就会发现，ZeRO-1/2 相比与 ZeRO-3， 实际上的区别仅在于 backward pass 时是否需要 AllGather parameters. ZeRO-1 和 ZeRO-2 保存了一份完整的参数，因此不需要 AllGather, 而 ZeRO-3 为了极致的内存优化，每次利用完参数之后就会直接释放掉，这导致其在反向传播时需要一次额外的 AllGather 来进行计算。
这体现了时间换空间的思想。

三者的内存和通信对比情况如下

|         Strategy         |       naive DP      |             ZeRO-1            |             ZeRO-2            |             ZeRO-3            |
|:------------------------:|:-------------------:|:-----------------------------:|:-----------------------------:|:-----------------------------:|
| Optimizer States per GPU |       $k\Psi$       |       $\frac{k\Psi}{N}$       |       $\frac{k\Psi}{N}$       |       $\frac{k\Psi}{N}$       |
|     Gradients per GPU    |       $2\Psi$       |            $2\Psi$            |       $\frac{2\Psi}{N}$       |       $\frac{2\Psi}{N}$       |
|    Parameters per GPU    |       $2\Psi$       |            $2\Psi$            |            $2\Psi$            |       $\frac{2\Psi}{N}$       |
|  Overall Memory per GPU  | $2\Psi+2\Psi+k\Psi$ | $2\Psi+2\Psi+\frac{k\Psi}{N}$ | $2\Psi+\frac{2\Psi+k\Psi}{N}$ | $\frac{2\Psi+2\Psi+k\Psi}{N}$ |
|       Communication      |          0          |            $2\Psi$            |            $2\Psi$            |            $3\Psi$            |
|          Models          |          -          |                               |                               |                               |
|           Notes          |                     |                               |                               |                               |

#### Zero-offload

ZeRO 的问题：只对参数，优化器状态，梯度有效，对于 activation 无效。
为了解决 activation 过大的问题，我们可以使用下一节提出的 TP 策略。
